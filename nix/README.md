# nix

Idk, could maybe have nixos instruction here ?
Link to me from the [main README](../README.md) ?

## TESTS

Run with: `nix flake check`

Create a CLI VM(virtual machine) that build the package and uses the modules.
[See for yourself](./tests/default.nix)
